class Planet {
  constructor (years) {
    this.merc = years / (0.24);
    this.venus = years / (0.62);
    this.mars = years / (1.88);
    this.jupiter = years / (11.86);
  }

  

}

export {Planet};
